# デプロイ手順（Vercel）

1. Vercel にログイン（未登録ならアカウント作成）

```bash
# npm 経由で CLI を使う（グローバルに入れたくない場合は npx を推奨）
npx vercel login
```

2. プロジェクトのルート（このリポジトリ）でデプロイ

```bash
# インタラクティブに進める（1回だけプロジェクト設定が必要）
npx vercel

# すぐに本番公開したい場合
npx vercel --prod
```

3. デプロイ後、CLI が公開 URL を出力します。ブラウザで開いて動作確認してください。

補足:
- 静的ファイルは `public/` 配下に配置されています。設定は `vercel.json` にあります。
- 継続的デプロイを有効にする場合は、Vercel 側で GitHub/GitLab のリポジトリを接続してください。
